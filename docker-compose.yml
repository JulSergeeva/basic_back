version: "3.8"
services:
  db:
    image: postgres:latest
    restart: always
    ports:
      - "32768:5432"
    volumes:
      - ./tmp/db:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password